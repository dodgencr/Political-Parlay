import {references} from '@/lib/references';
export default function References(){return <main className="references-page"><div className="eyebrow">FOLLOW THE SOURCE</div><h1>References & methodology</h1><p>Every figure needs a source. Every source needs context.</p>{references.map((r,i)=><article className="reference-row" key={r.name}><h2><a href={r.url} target="_blank" rel="noreferrer"><span className="eyebrow">{String(i+1).padStart(2,'0')} / </span>{r.name} ↗</a></h2><p>{r.note}</p></article>)}<section className="method"><h2>How to read the dashboard</h2><p>Campaign receipts, outside spending, lobbying expenses and personal wealth measure different things. We do not add them together or calculate an influence score. A disclosed relationship or a name match is not evidence of wrongdoing.</p><h3>Lobbyist matching</h3><p>LD-203 searches cover the 2026 filing year, matching first and last names in honoree or payee fields. Aliases can be missed and unrelated people can share names. Check the original filing and actual payee. We use the latest returned filing per filer and period; identical disclosed entries are grouped with source links retained. Searches are capped at four pages per field, and capped results are labeled partial.</p><h3>What sync can—and cannot—do</h3><p>Sync fetches published records and preserves previous results when a source fails. Filing delays, amendments and rate limits affect coverage. It cannot reveal unreported money. House documents are indexed, but their contents have not been analyzed. Senate personal disclosures and travel require manual review.</p><h3>District map</h3><p>The map uses simplified Census boundaries for the 119th Congress (2025–26). It links to currently serving members in our roster. The boundary snapshot does not change when you sync, and should not be used to determine your district in a future election.</p></section></main>}
